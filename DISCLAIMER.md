# 数据来源与免责声明

## 关于标准文本

本仓库中的 `standards/*.yaml` 包含工程建设标准（如 GB 50222-2017《建筑内部装修设计防火规范》）的条文内容。请注意：

1. **非官方发布渠道**。本仓库不是国家标准的发行或转载渠道，数据由 PDF/OCR 文本经 **AI 辅助的自动化提取** 与 **人工逐页校对** 整理而成——虽经机器审计与人工复核，**仍可能存在缺漏、错字或断句偏差**。
2. **请以官方文本为准**。用于工程设计、审查、施工、验收等正式场合时，必须回溯到现行有效版本的官方出版物；本仓库数据仅供学习、研究与工具链验证之用。
3. **权利归属**。标准文本的著作权与专有出版权归相应标准化主管部门与出版单位所有。本仓库转载条文内容出于研究交流目的；如权利人认为构成侵权，请提交 issue 或邮件联系，我将在核实后第一时间删除相关内容。

## 一键剔除条文原文

若你所在的环境不允许处理或展示标准原文，仓库提供了剔除脚本，保留全部结构化元数据（编号、适用条件、参数、表格、规则），仅清空原文类字段：

```bash
python tools/strip_text.py standards/GB50222-2017.yaml standards/GB50222-2017_notext.yaml
```

被清空的字段：

- `clauses[].text`
- `tables[].content`
- `executable_rules[].source_text`
- `references[].original_text`

保留的字段：条文编号、章节、废止状态、适用范围与条件、参数阈值、表格结构（`headers` / `rows`）、引用编号、IFC 映射、可执行规则等。**这仍然是完整可用的结构化数据**——绝大多数检索、比对、规则判定场景不需要原文。

## 关于本仓库的代码与工具

`tools/` 目录下的脚本与阅读器在 MIT 许可证下发布，详见 [LICENSE](LICENSE)。MIT 授权**不覆盖** `standards/` 下的标准文本数据。
