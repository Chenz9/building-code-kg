# MODAL_NULL_BUT_MANDATORY 白名单（人工裁定）
# 背景：v2.1→v2.2 迁移后，出现「is_mandatory_clause=true 且 strictness=无/none」的条文。
#      经逐条回源判定，其中「目的/宗旨条」在全文强制规范中本就无约束语气，属合法状态，
#      故豁免审计，而非数据错误。
# 格式：条文id | 类别 | 说明（脚本只取 '|' 前的内容作 id）
# 类别 A = 目的/宗旨条 / 罚则条 —— 合法状态，无需修复
#
# 说明：本文件仅收录本仓库已开源标准相关的豁免条目。
#       私有工作区另有 30 余册的完整白名单（含 B 类源文本残缺、C 类疑似强条误标），
#       随各册完成人工校对并开源后同步。

# ---- A 类：目的/宗旨条 ----
GB 55031-2022#1.0.1 | A | 目的条「为规范…制定本规范」，全文强制规范，无约束语气属正常
