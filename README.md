# 建筑规范结构化知识库（Building Code Knowledge Base）

把中文工程建设标准（国标 GB / 行标 JGJ 等）从 PDF/OCR 文本，**转成机器可读、可被设计软件与审查流程直接消费的 YAML 结构化数据**：条文编号、适用条件与例外、数值阈值、查表逻辑、强条与废止状态、IFC 对象映射、可执行规则。

本仓库目前收录 **32 本标准**的结构化 YAML，按数据成熟度分为两档，请按需取用：

| 档位 | 数量 | 状态 |
|---|---|---|
| **深度复核样板** | 1 本：GB 50222-2017 | OCR 交叉识别 + 官方 PDF 逐页回源核验 + 两套机器审计均已完成；**全册逐条人工校对仍在进行中，尚未完成** |
| **结构化初稿** | 31 本 | 机器抽取完成、v2.1 结构层审计 0 违规；**人工校对尚未开始，内容差错风险显著高于样板**，正逐册推进 |

> ⚠️ 标准文本的权利声明与免责条款见 [DISCLAIMER.md](DISCLAIMER.md)，使用前请先阅读。
>
> ⚠️ **「审计 0 违规」指结构层面的机器校验，不等于内容语义零差错。** 2026-09-08 的外部评审就在机检全 0 的数据里发现了 4 处实质错误——本仓库已完成整改并如实公示（见 [§6 整改记录](#6-质量门禁与整改记录)）。涉及工程决策前，请务必回源核对官方版本。

---

## 1. 为什么做这件事

工程设计行业信息化程度整体偏低，规范校验长期依赖人工完成，而代价最高的一类错误往往不是计算失误，而是规范读漏、读错。一份建筑总说明里散落着数十条引用规范，条文冲突、版本错配、图纸与规范不符等问题，常到施工图审查甚至现场返工才暴露，造成工期与成本损失。

这类问题在行业内十分常见：比如误用已废止的规范、跨专业条文要求矛盾、图纸标注与规范条文不一致等，人工逐条核对极易遗漏。

要从根源解决问题，核心是让规范的形态从「给人读的PDF」转向「给程序读的结构化数据」。本仓库就是把每条规范拆解为可被程序识别判定的最小单元，实现自动校验，将错误拦截在设计前期：

| 需求 | 结构化之后能做到 |
|---|---|
| 设计说明里的引用条文是否齐全 | 每条带 `standard_code` + `clause_no`，可精确比对清单 |
| 这条规定对我这个部位适不适用 | `applicability.scope` / `conditions` / `exceptions` 三元组 |
| 我的取值达标了吗 | `parameters[].min_value/max_value/unit` |
| 该查哪张表 | `tables[].headers/rows`（样板册 6 张表 79 行已结构化） |
| 是不是强条 / 有没有被废止 | `constraint.is_mandatory_clause` + `is_abolished` + `superseded_by` |
| 能不能进 BIM 审查 | `related_objects.component_types` 映射到 IFC 实体（当前为保守宽匹配，逐条精修在路线图） |
| 能不能直接被规则引擎跑 | `executable_rules`（when → then） |

## 2. 仓库结构

```
.
├── README.md                      # 你正在看的文件
├── DISCLAIMER.md                  # 标准文本来源说明与免责条款
├── LICENSE                        # MIT（仅覆盖代码与工具，不含标准文本）
├── schema/
│   └── SCHEMA_v2.1.yaml           # 数据结构定义（权威版本，带书写规约）
├── standards/
│   ├── GB50222-2017.yaml          # 深度复核样板：49 条条文的结构化数据
│   └── *.yaml                     # 其余 31 本：结构化初稿（人工校对进行中）
└── tools/
    ├── yaml-reader.html           # 通用 YAML 阅读器（离线单文件，零依赖）
    ├── audit_v21.py               # v2.1 合规审计脚本（开源，可独立复跑验证）
    ├── gen_readview_html.py       # 由 YAML 生成静态阅读视图的脚本
    └── strip_text.py              # 合规阀：一键剔除条文原文，仅留结构化元数据
```

## 3. 数据结构速览

单份 YAML 由三层组成：`standard`（整本元数据）、`clauses`（逐条条文）、`executable_rules`（可判定规则）。完整字段定义与书写规约见 [`schema/SCHEMA_v2.1.yaml`](schema/SCHEMA_v2.1.yaml)。

```yaml
standard:                # 整本：编号、名称、实施/废止日期、强条数、相关规范、schema 版本
  code: GB 50222-2017
  clauses: [...]         # 条文 id 清单
clauses:
  - id: GB 50222-2017#3.0.4
    clause_no: 3.0.4
    chapter: 3 装修材料的分类和分级
    text: 安装在金属龙骨上燃烧性能达到B1级的纸面石膏板、矿棉吸声板，可作为A级装修材料使用。
    is_abolished: false
    superseded_by: null
    applicability:       # 「对谁有效」——主体 / 条件 / 排除
      scope: 安装在金属龙骨上的纸面石膏板、矿棉吸声板
      conditions: [燃烧性能达到 B1 级]
      exceptions: []
    constraint:          # 「多强」——情态动词 + 是否强条 + 依据
      modal_level: may           # must / should / may
      modal_words: [可]
      is_mandatory_clause: false
    parameters:          # 「到什么程度」——五字段统一签名
      - name: 纸面石膏板/矿棉吸声板燃烧性能等级
        unit: null
        condition: 安装在金属龙骨上
        min_value: null
        max_value: null
    tables: []           # 查表类条款：结构化 headers + rows
    references: []       # 跨标准引用 + 处置状态（resolved / unresolved）
    related_objects:     # IFC 实体与活动类型映射
      component_types: [IfcCovering, IfcWall, IfcCeiling]
      activity_types: [内部装修防火设计]
executable_rules:        # 面向规则引擎：when 条件数组 → then 结论
  - rule_id: R_GB50222_3_0_4_board_grade
    when: ["material IN ['纸面石膏板','矿棉吸声板']", "fire_performance == 'B1'", "install == '金属龙骨'"]
    then: { result_grade: A }
```

**几条立规矩的经验**（都是踩过坑才定下来的）：

- **参数必须五字段齐名**：`name / unit / condition / min_value / max_value`，缺 unit 也要显式 `null`。这是所有下游脚本的字段签名。
- **「除 X 外」不写进 `exceptions`**：那是 SQL 式排除思维。范围限定应并入 `scope`（限定主体）或 `conditions`（条件分支），`exceptions` 只放被排除对象本身。
- **等级型/枚举型条款不是阈值约束**。像「B1 级可视同 A 级」这种等效规则，`>=` 数值比较是错的——参数层用 `condition` + `_v21_param_note` 说明，真正的换算逻辑放 `executable_rules`。
- **不要发明新字段名**。`operator` / `value` 这类自定义键不会被任何脚本识别，也不会进规则镜像。

## 4. 本册数据概况：GB 50222-2017

| 项 | 值 |
|---|---|
| 标准号 / 名称 | GB 50222-2017《建筑内部装修设计防火规范》 |
| 实施日期 | 2018-04-01 |
| 领域 | 防火、建筑装修、消防设计 |
| 条文总数 | 49 |
| 强制性条文 | 18 条（**全部已被 GB 55037-2022 废止效力**，逐条标注 `superseded_by`） |
| 适用范围填充 | 49 / 49（100%） |
| 带适用条件的条文 | 34 |
| 带例外的条文 | 9 |
| 情态分布 | must 31 / may 11 |
| 结构化表格 | 6 张，共 79 行（已按定义条文归位；另有 2 张已转文本） |
| 数值参数 | 5 |
| 跨标准引用 | 2（GB 50016 协调、GB 8624 材料分级） |
| 可执行规则 | 13 条（其中 4 条参数镜像已于 2026-09-08 人工校正参数名与阈值方向） |
| 文件体积 | 77 KB |

章节分布：总则 4 / 术语 4 / 装修材料的分类和分级 7 / 特别场所 20 / 民用建筑 9 / 厂房仓库 5。

**上位替代关系**：GB 55037-2022《建筑防火通用规范》（全文强制）发布后，本规范全部强制性条文的效力已被废止——本册数据对此做了逐条标注，检索时不会被误导去执行已失效条文。

## 5. 怎么用

### 5.1 最省事：拖进阅读器

打开 [`tools/yaml-reader.html`](tools/yaml-reader.html)（下载后双击，或在本仓库页面直接预览），把 `standards/GB50222-2017.yaml` 拖进窗口即可。

- **零依赖**：内置纯 JS YAML 解析器，不联网、不需要 Python/Node，file:// 下直接可用
- **书模式**：自动识别 `standard` + `clauses` 结构，渲染封面信息、章节目录、强条/废止徽标、适用范围/条件/例外、参数阈值表、原表格、引用与规则（「当…且… → 则…」）
- **树模式**：任意 YAML 的通用层级树，支持路径面包屑与值复制
- 支持多文件/整目录加载、实时搜索高亮、按强条/废止/参数/条件/表格/引用筛选、字号与深浅主题

该解析器已与 PyYAML 对内部知识库 34 份 YAML（32 本正本 + 2 份 schema 历史基线，均在私有工作区维护，本仓库随整改逐步同步）做过**逐字段一致性比对，零差异**。

### 5.2 用 Python 读数据

```python
import yaml
d = yaml.safe_load(open('standards/GB50222-2017.yaml', encoding='utf-8'))

for c in d['clauses']:
    if c['constraint']['is_mandatory_clause'] and not c['is_abolished']:
        print(c['clause_no'], c['text'])
```

如果需要一份**静态分发版**（不依赖阅读器，可直接发给别人双击打开）：

```bash
pip install pyyaml
python tools/gen_readview_html.py standards/GB50222-2017.yaml 阅读视图.html
```

生成的是自包含单文件 HTML，输出到哪里都可以，不必放进本仓库。

## 6. 质量门禁与整改记录

结构化数据最怕「看起来齐了」。所以每一步都设有机器门禁；但也要如实说明：**机器门禁 ≠ 语义零差错**。

### 样板册（GB 50222-2017）当前状态

| 门禁 | 内容 | 结果 |
|---|---|---|
| **V1–V7** | 必填完整性、编号唯一性、 OCR 截断、弱提取、 适用性缺失、对象缺失 | 全 0 |
| **v2.1 合规** | 参数名语义、单位词典、排除项书写风格、强条归位、规则镜像一致性 | 全 0（可独立复跑：`python tools/audit_v21.py`） |
| **V4 适用范围** | 每条人工回填「对谁有效」 | 49/49 |
| **回源核验** | 官方 PDF 120 DPI 渲染逐页视觉比对 + OCR 双源交叉 | 已完成两轮（38 处 + 8 处修正合入） |
| **全册逐条人工校对** | 逐条语义级人工确认 | **进行中，尚未完成** |

其余 31 本目前仅通过前两项机器门禁，人工校对尚未开始——引用前请自行回源核对。

### 2026-09-08 外部评审整改记录

外部评审在机检全 0 的数据中发现了实质错误。以下问题全部整改完毕：

| 评审发现 | 整改 |
|---|---|
| 3.0.6「涂层干膜厚度**不大于** 1.0mm」被标成 `min_value: 1.0`（方向反，会导致合规判定反判） | 参数与规则镜像同步改为 `max_value: 1.0` |
| 4.0.16 参数名「材料的距离」为 OCR 碎片 | 更名「高温部位与装修材料距离」，规则镜像同步 |
| 3 条自动规则参数名碎片（「位面积质量」缺字、「上湿涂覆比」多字） | 已修正，note 从「待人工精修」更新为「已人工校正」 |
| `standard.clauses` 索引中 5.1.2 排在 5.1.1 前 | 已排序修正 |
| extraction_notes 残留过期矛盾记录（「疑似残缺 5.1.1」与修复记录并存） | 已清理过期项，追加整改记录 |

**门禁盲区教训**：阈值方向反写（min/max 颠倒）、参数名截断碎片混入规则镜像、审计日志过期——这三类此前不在机检规则覆盖范围内，机检全 0 依然漏网。方向类校验规则将纳入审计脚本下一版。

（评审同时指出的情态词误标、4.0.6「开散楼梯」错别字等项，经回查现行版本均不存在，应为评审对象为更早版本所致；如有发现欢迎开 issue 指认。）

## 7. 生成方式与工具链

这套数据既不是纯手写，也不是纯自动生成，走的是**「LLM 辅助抽取 → 机器审计 → 人工精校」**三段式：

| 阶段 | 承担者 | 做什么 |
|---|---|---|
| 初筛与抽取 | 大语言模型 | 从 PDF/OCR 文本中识别条文边界、情态词、数值参数、表格结构，产出 YAML 草稿 |
| 机器审计 | 审计脚本 | 按 `SCHEMA_v2.1` 与 V1–V7 规则逐条扫描，把问题收敛成可机检清单 |
| 人工精校 | 人 | 标准 PDF 按 120 DPI 全本渲染后逐页视觉比对，处理机器判不了的部分（语义归属、等级型条款、误抽与幽灵条文） |

本项目的绝大部分内容，是在 **WorkBuddy** 与 **Trae** 两个 AI 编码环境中完成的，底层调用了 **QWEN3.7、KIMI K3、GLM5.3** 等大语言模型。

这里有一点值得强调：**这套数据模型是模型无关的**。无论换用哪个模型参与抽取，产出都必须通过同一份 `schema/SCHEMA_v2.1.yaml` 和同一套审计脚本——结构由模式约束，而不是由某个模型的输出习惯决定。这正是我们敢于让多个模型参与、并把结果整合进同一套数据的前提：模型负责提效，模式和审计负责收敛质量。

## 8. 路线图

- [x] 全库 32 本首批开源（2026-09-08）——其中 31 本为**结构化初稿**，人工校对进行中
- [ ] **逐册完成人工校对**，从「结构化初稿」升级为「深度复核」档（防火、防水、节能、无障碍、隔声、热工、门窗、玻璃、屋面、车库、剧场、展览、电气、消防设施、施工质控、数据中心等）
- [ ] 开源 V1–V7 审计脚本，并把阈值方向校验（min/max 反写检测）等本次整改沉淀的新机检规则纳入
- [ ] IFC 映射逐条精修：当前 rules 的 `when` 中 `component_type` 为保守宽匹配（四类实体全量覆盖），尚不构成精确的 BIM 构件级映射
- [ ] 提供 English field dictionary，便于国际工具链对接
- [ ] 跨标准冲突检测（`conflicts` 字段目前尚未批量填充）
- [ ] 与设计说明提取对接：自动比对「说明里写了」与「规范里要求」

## 9. 引用

如果这套结构对你的研究或产品有帮助，欢迎引用并提 issue 交流。仓库也接受 issue 形式的勘误——标准数据天然需要众人校对。
